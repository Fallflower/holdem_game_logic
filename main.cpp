#include "game.h"

int main() {
    Game g(9, 2);
    g.show();

    // test
    // Position p(9, 1);
    // std::cout << p << std::endl;
    // Game g(9, 2);
    // std::cout << g.getPot() << std::endl;
    return 0;
}